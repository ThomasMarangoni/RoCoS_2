"""
Here additional and generaic methods are placed, so they can be used in multiple files.
"""

# converts a string to a float value. The needed format is a . as floating point.
def convertFloat(value):
    stringValue = str(value)
    return float(stringValue)

# converts a string to a float value
def convertInt(value):
    stringValue = str(value)
    return int(stringValue)